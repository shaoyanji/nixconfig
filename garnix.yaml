builds:
  # exclude:
  # - homeConfigurations.*
  # - darwinConfigurations.*
  # - nixosConfigurations.*
  include:
    - nixosConfigurations.garnixMachine
    - nixosConfigurations.testvm
servers:
  - configuration: garnixMachine
    deployment:
      type: on-branch
      branch: main
