builds:
  # exclude:
  # - homeConfigurations.*
  # - darwinConfigurations.*
  # - nixosConfigurations.*
  include:
    - nixosConfigurations.garnixMachine
servers:
  - configuration: garnixMachine
    deployment:
      type: on-branch
      branch: main
