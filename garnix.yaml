builds:
  # exclude:
  # - homeConfigurations.*
  # - darwinConfigurations.*
  # - nixosConfigurations.*
  include:
    - nixosConfigurations.*
# - nixosConfigurations.schneeeule
servers:
  - configuration: garnixMachine
    deployment:
      type: on-branch
      branch: main
